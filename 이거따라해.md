# Vast.ai 새 인스턴스에서 NetLLM 환경 다시 만들기

이 문서는 Vast.ai 인스턴스를 Destroy한 뒤, 새 인스턴스에서 기존 NetLLM 실험 환경을 다시 만드는 방법입니다.

개발을 잘 몰라도 괜찮습니다. 아래 순서대로 터미널에 복사해서 붙여넣으면 됩니다.

## 0. 전체 흐름

해야 할 일은 총 4개입니다.

1. GitHub private repo에 접근할 수 있게 SSH 키 만들기
2. GitHub에 SSH public key 등록하기
3. Vast.ai 새 인스턴스에 setup repo clone하기
4. `setup.sh` 실행해서 환경 복구하기

## 1. SSH 키 만들기

Vast.ai 터미널에서 아래 명령어를 실행합니다.

```bash
ssh-keygen -t ed25519 -C "soyuniverse@github"
```

그러면 이런 질문이 나옵니다.

```text
Enter file in which to save the key (/root/.ssh/id_ed25519):
```

여기서는 아무것도 입력하지 말고 **Enter**를 누릅니다.

다음 질문이 나옵니다.

```text
Enter passphrase (empty for no passphrase):
```

이것도 아무것도 입력하지 말고 **Enter**를 누릅니다.

다시 한 번 나옵니다.

```text
Enter same passphrase again:
```

이것도 아무것도 입력하지 말고 **Enter**를 누릅니다.

성공하면 대충 이런 화면이 나옵니다.

```text
Your identification has been saved in /root/.ssh/id_ed25519
Your public key has been saved in /root/.ssh/id_ed25519.pub
```

## 2. GitHub에 넣을 public key 복사하기

아래 명령어를 실행합니다.

```bash
cat ~/.ssh/id_ed25519.pub
```

그러면 이런 식으로 한 줄이 나옵니다.

```text
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... soyuniverse@github
```

이 한 줄 전체를 복사합니다.

중요합니다.

- `ssh-ed25519`부터 시작해서
- 맨 끝 `soyuniverse@github`까지
- 한 줄 전체를 복사해야 합니다.

절대 복사하면 안 되는 파일:

```text
/root/.ssh/id_ed25519
```

GitHub에 등록하는 것은 `.pub` 파일 내용입니다.

```text
/root/.ssh/id_ed25519.pub
```

## 3. GitHub에 SSH key 등록하기

브라우저에서 GitHub에 들어갑니다.

```text
GitHub -> 오른쪽 위 프로필 사진 -> Settings
```

왼쪽 메뉴에서:

```text
SSH and GPG keys
```

를 누릅니다.

그 다음:

```text
New SSH key
```

를 누릅니다.

입력칸은 이렇게 채웁니다.

```text
Title: vast-ai-netllm
Key: 아까 복사한 ssh-ed25519 ... soyuniverse@github 한 줄 전체
```

마지막으로:

```text
Add SSH key
```

를 누릅니다.

## 4. SSH 연결 테스트하기

다시 Vast.ai 터미널로 돌아와서 아래 명령어를 실행합니다.

```bash
ssh -T git@github.com
```

처음 연결이면 이런 질문이 나올 수 있습니다.

```text
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

여기에는 아래처럼 입력하고 Enter를 누릅니다.

```text
yes
```

성공하면 이런 식으로 나옵니다.

```text
Hi soyuniverse! You've successfully authenticated...
```

정확히 똑같지 않아도 됩니다. `successfully authenticated`라는 말이 보이면 성공입니다.

## 5. setup repo clone하기

이제 아래 명령어를 순서대로 실행합니다.

```bash
cd /workspace
git clone git@github.com:soyuniverse/NetLLM.git NetLLM
cd NetLLM
```

이 setup repo는 `/workspace/NetLLM`에 둬도 됩니다.
`setup.sh`가 원본 NetLLM 코드는 `/workspace/NetLLM-source`에 따로 받습니다.

## 6. 환경 복구하기

아래 명령어를 실행합니다.

```bash
bash setup.sh
```

이 작업은 시간이 걸릴 수 있습니다.

설치되는 것:

- NetLLM 원본 코드
- Python 3.8.10 conda 환경
- PyTorch 2.1.0
- viewport prediction에 필요한 Python 패키지들
- Codex CLI 설치 시도
- Jupyter / VS Code 관련 상태 확인

끝까지 완료되면 `Setup complete` 같은 문구가 나옵니다.

## 7. 환경 점검하기

설치가 끝나면 아래 명령어를 실행합니다.

```bash
bash scripts/check_env.sh
```

확인할 것:

- `cuda available: True`
- GPU 이름이 보이는지
- `cv2` 버전이 보이는지
- `yacs import: ok`가 보이는지

## 8. 실험 스크립트 실행하기

CPU baseline 테스트:

```bash
bash scripts/run_vp_regression_cpu.sh
```

GPT-2 adaptation 1 epoch 실행:

```bash
bash scripts/run_vp_gpt2_adapt_e1.sh
```

GPT-2 adaptation은 백그라운드에서 실행됩니다.

실행 후 화면에 로그 파일 경로가 나옵니다. 로그를 보고 싶으면 화면에 나온 `tail -f ...` 명령어를 복사해서 실행하면 됩니다.

## 9. 자주 나는 문제

### GitHub clone이 안 되는 경우

에러 예시:

```text
Permission denied (publickey).
```

해결:

- `cat ~/.ssh/id_ed25519.pub`로 나온 public key를 GitHub에 등록했는지 확인
- GitHub에 등록한 key가 `ssh-ed25519 ... soyuniverse@github` 한 줄 전체인지 확인
- 다시 테스트:

```bash
ssh -T git@github.com
```

### GitHub username/password를 물어보는 경우

SSH 주소가 아니라 HTTPS 주소를 쓰고 있는 것입니다.

올바른 주소:

```text
git@github.com:soyuniverse/NetLLM.git
```

잘못된 주소:

```text
https://github.com/soyuniverse/NetLLM.git
```

### 데이터셋 문제로 실험이 실패하는 경우

`setup.sh`는 코드와 실행환경을 복구합니다.

하지만 아래 파일들은 자동 복구하지 않습니다.

- 데이터셋
- checkpoint
- 직접 다운로드한 GPT-2/LLM 모델
- 이전 실험 로그

그래서 환경 설치는 성공했는데 실험이 데이터 파일 없음으로 실패할 수 있습니다. 이 경우는 setup 문제가 아니라 데이터 준비가 필요한 상황입니다.

## 10. Destroy 전에 꼭 확인할 것

Vast.ai 인스턴스를 Destroy하면 인스턴스 안의 파일은 삭제됩니다.

Destroy 전에 중요한 코드는 GitHub에 push되어 있어야 합니다.

확인 명령어:

```bash
git status
```

깨끗한 상태면 보통 이런 문구가 나옵니다.

```text
nothing to commit, working tree clean
```

새로 수정한 파일이 있으면 아래 순서로 GitHub에 올립니다.

```bash
git add .
git commit -m "Update setup files"
git push origin main
```

## 11. 핵심 요약

새 Vast.ai 인스턴스에서 가장 중요한 명령어는 이것입니다.

```bash
ssh-keygen -t ed25519 -C "soyuniverse@github"
cat ~/.ssh/id_ed25519.pub
```

GitHub에 public key 등록 후:

```bash
cd /workspace
git clone git@github.com:soyuniverse/NetLLM.git NetLLM
cd NetLLM
bash setup.sh
bash scripts/check_env.sh
```
